---
name: svn-patch-merge
description: 当需要将SVN或git补丁合并到Android平台客户化项目时使用，仅合并补丁和客户化项目中都存在的文件
allowed-tools: [Read, Write, Bash, FileExists, Glob]
---

# SVN 补丁智能合并专家

## 核心目标
将Android平台代码更新生成的对应补丁`patch.diff`合并到目标项目中。


## 核心规则
1. **合并策略**：
   - 尽量同时保留补丁中的逻辑和目标代码中的逻辑。
   - 存在冲突的代码，不涉及判断逻辑的则以补丁为准，涉及判断逻辑的则以客户化为准。
   - 目标文件已经存在相同逻辑的代码，则不用合。
2. **安全机制**：
   - 修改文件前，**必须**先创建备份（例如 `filename.bak`），合并成功后再删除备份。

## 快速参考

补丁文件的处理决策（按优先级）：

| 条件 | 处理方式 |
|------|----------|
| 目标项目中不存在该文件 | 跳过 |
| 文件为二进制格式 | 跳过，记入”需人工合并” |
| 文本文件，修改状态为”删除” | 跳过，记入”需人工合并” |
| 文本文件，修改状态为”修改”或”新增” | 执行选择性合并 |

## 执行步骤

### 1. 确认补丁来源

> **交互规则**：本步骤涉及用户选择或输入时，**必须**使用 `AskUserQuestion` 工具，禁止用纯文本提问或自行猜测。

按以下优先级处理：

**情况A**：用户在消息中已指定补丁文件路径 → 直接进入步骤2预处理。

**情况B**：用户未指定补丁文件，但项目根目录存在 `*.diff` 文件 →
1. `Bash` 执行 `ls *.diff` 列出文件
2. 调用 `AskUserQuestion`：
   - `header`: `"选择补丁"`
   - `question`: `"项目根目录下找到以下补丁文件，请选择要合并的补丁："`
   - `options`: 每个 `.diff` 作为一个选项（超过4个时只列前4个，其余合并为"其他"选项）

**情况C**：未找到任何 `.diff` 文件 →
1. 调用 `AskUserQuestion`（一次调用包含3个问题）：
   - 问题1: `header`: `"版本范围"`, `question`: `"请提供起始版本号（格式: startversion:endversion，如 371477:HEAD）"`
   - 问题2: `header`: `"SVN账号"`, `question`: `"请提供 SVN 账号"`
   - 问题3: `header`: `"SVN密码"`, `question`: `"请提供 SVN 密码"`
2. 获取到信息后，在平级 `platform` 目录执行 svn diff 生成补丁，拷贝到目标项目根目录。

   **重要**：执行 `svn diff` 时必须 `cd` 到 `platform` 目录内部，使用 `.` 作为目标路径（而非绝对路径），这样生成的 `Index:` 行就是相对路径：
   ```
   cd /path/to/platform && svn diff -r START:END --username USER --password PASS . > /path/to/project/patch.diff
   ```

### 2. 预处理：拆分补丁
运行脚本自动检测补丁格式(SVN diff / Git diff / Git format-patch)，按文件拆分、识别修改状态和文件类型，匹配目标项目中存在的文件，生成 `files_need_merge.json`：

```
python3 ~/.claude/skills/svn-patch-merge/scripts/distill_patches.py patch.diff
```

### 3. 逐文件合并
读取 `files_need_merge.json`，按”快速参考”表中的决策逐文件处理。合并时注意根据补丁上下文将代码插入正确位置。遇到任何问题（如文件权限）自行解决，不让用户介入。

### 4. 验证
`svn st | grep ^M` 检查实际修改文件列表，不符合预期则继续调整。

### 5. 查看目标项目下是否存在子项目
> **交互规则**：本步骤涉及用户选择时，**必须**使用 `AskUserQuestion` 工具。

- `Bash` 执行 `ls sk_branches 2>/dev/null` 检查是否存在 `sk_branches` 目录。
- 不存在则跳过本步骤。
- 存在则：
  1. 调用 `AskUserQuestion`（`multiSelect: true`），列出sk_branches下的所有子项目供用户多选。
  2. 用户选择后，逐个 `cd` 进入选中的子项目目录，按步骤 2、3、4 执行合并（补丁文件使用项目根目录的同一份 `patch.diff`）。


### 6. 输出报告
输出合并统计：成功合并数、跳过数、需人工合并数。

## 常见错误与危险信号

### 常见错误

| 错误做法 | 正确做法 |
|----------|----------|
| 遇到权限等问题让用户人工处理 | 自行解决（chmod、修改文件属性等），不让用户介入 |

### 危险信号

出现以下想法意味着正在走偏，立即纠正：

- "权限问题让用户自己解决吧" → 自行解决，永远不让用户介入
- "格式检测不出来，猜一种格式试试" → 立即停止，询问用户

### 解析失败处理
补丁格式无法自动识别或解析时，立即停止并询问用户，不要猜测格式。


## 使用方式
用户输入：请合并 patch.diff 到当前项目