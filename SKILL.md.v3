---
name: svn-patch-merge
description: 智能合并SVN补丁，仅合并补丁和项目中都存在的文件
allowed-tools: [Read, Write, FileExists, Glob]
---

# SVN 补丁智能合并专家

## 🎯 核心目标
将 `patch.diff` 中的代码变更合并到当前目标项目中，**严格保持目标项目的文件结构不变**。

## 🛡️ 核心规则
1. **合并策略**：
   - 尽量同时保留补丁中的逻辑和目标项目代码中的逻辑
   - 如果有冲突的逻辑或者相反的逻辑，则以客户化逻辑为主
2. **安全机制**：
   - 修改文件前，**必须**先创建备份（例如 `filename.bak`），合并成功后再删除备份。

## 🛠️ 执行步骤
1. **自动化预处理（调用本地脚本），解析补丁、标记补丁中文件状态、保存到json**：
   - 直接运行项目中的预处理脚本来完成解析、分割和检查。
   - 执行命令：`python3 scripts/split_diff.py patch.diff`，请根据实际的原始补丁文件名替代patch.diff。
   - 脚本执行后，读取并展示脚本输出的统计结果或分类表格。
2. **循环处理**：
   - 读取补丁对应目录下的json文件，例如patch/files_info.json，检查目标项目下是否存在对应的文件。
   - **存在** → 读取原文件状态，如果是“新增的”或者“删除的”，则记录到记录到统计报告，类型为“需要人工合并”。如果文件为二进制文件则不用合并，记录类型为“需要人工合并”。如果是“修改的”，则直接合并补丁到目标文件。
   - **不存在** → 记录“跳过”。
3. **生成报告**：
   - 输出合并统计：成功合并数、跳过数、需要人工合并的文件数。

## ⚠️ 注意事项
- 如果原始完整补丁格式无法解析，请立即停止并询问用户。


## 使用方式
用户输入：请合并 patch.diff 到当前项目