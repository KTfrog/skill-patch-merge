---
name: svn-patch-merge
description: 智能合并Android平台代码SVN补丁，仅合并补丁和客户化项目中都存在的文件
allowed-tools: [Read, Write, Bash, FileExists, Glob]
---

# SVN 补丁智能合并专家

## 🎯 核心目标
将Android平台代码更新生成的对应补丁`patch.diff`合并到目标项目中。

## 🛡️ 核心规则
1. **合并策略**：
   - 尽量同时保留补丁中的逻辑和目标代码中的逻辑。
   - 存在冲突的代码，不涉及判断逻辑的则以补丁为准，涉及判断逻辑的则以客户化为准。
   - 目标文件已经存在相同逻辑的代码，则不用合。
2. **安全机制**：
   - 修改文件前，**必须**先创建备份（例如 `filename.bak`），合并成功后再删除备份。

## 🛠️ 执行步骤
1. **自动化预处理（调用本地脚本），解析原始完整补丁、标记补丁中每个文件状态、保存到json**：
   - 直接运行项目中的预处理脚本来完成解析、文件匹配、提取元数据和补丁。最终生成补丁元数据索引patch/files_need_merge.json。
   - 执行命令：`python3 ~/.claude/skills/svn-patch-merge/scripts/distill_patches.py patch.diff`，请根据实际的原始补丁文件名替代patch.diff。
2. **读取files_need_merge.json，合并对应补丁到目标项目中**
   - 目标项目中不存在的文件，不论其是什么类型的文件，直接跳过。
   - 如果文件是“二进制的”，则不要合并，记录到“需要人工合并”。
   - 如果文件是“文本的”，但是修改状态是“删除的”，不要合并，记录到“需要人工合并”。
   - 只有文件是“文本的”，且修改状态是“修改的”和“新增的”，则直接执行合并（注意查看补丁的上下文，把补丁代码插入到正确的位置），执行合并过程中不论遇到什么问题（例如文件权限问题）都尝试自己解决问题，永远不要让用户进行人工合并。
3. **验证**  
使用svn st | grep ^M获取哪些文件真正被修改了，验证是否符合预期。如果不符合预期则继续合并代码。
4. **生成报告**：
   - 输出合并统计：成功合并数、跳过数、需要人工合并的文件数。

## ⚠️ 注意事项
- 如果原始完整补丁格式无法解析，请立即停止并询问用户。


## 使用方式
用户输入：请合并 patch.diff 到当前项目