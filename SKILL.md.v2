---
name: svn-patch-merge
description: 智能合并SVN补丁，仅合并补丁和项目中都存在的文件
allowed-tools: [Read, Write, FileExists, Glob]
---

# SVN 补丁智能合并专家

## 🎯 核心目标
将 `patch.diff` 中的代码变更合并到当前目标项目中，**严格保持目标项目的文件结构不变**。

## 🛡️ 核心规则
1. **合并策略**：
   - 尽量同时保留补丁中的逻辑和目标项目代码中的逻辑
   - 如果有冲突的逻辑或者相反的逻辑，则以客户化逻辑为主
2. **安全机制**：
   - 修改文件前，**必须**先创建备份（例如 `filename.bak`），合并成功后再删除备份。

## 🛠️ 执行步骤
1. **解析补丁，分割大补丁为小补丁，标记状态**：
   - 读取diff补丁文件（例如 `xxx.diff`），提取所有涉及的文件路径（通常以`Index:`开头）。
   - 然后创建根据补丁文件的名字（例如xxx.diff，则创建xxx目录）创建对应的目录，然后把补丁中每一个的文件的改动都提取出来对应一个小补丁文件存放到创建的目录中，命名从1-文件名.diff，2-文件名.diff等等。
   - 每一个小补丁文件都单独并标记其状态，是新增的、被删除的、被修改的。然后输出一个表格，让用户确认。确认OK后执行下一步。
2. **循环处理**：
   - 挨个读取小补丁，检查目标项目是否存在对应的文件。
   - **存在** → 读取原文件状态，如果是“新增的”或者“删除的”，则记录到“需要人工合并”。如果文件为二进制文件则不用合并，记录到统计报告中，类型为“需要人工合并”。如果是“修改的”，则把小补丁追加写到needmerge.diff中，待用户确认后才开始合并。
   - **不存在** → 记录“跳过”。
   输出一个表格，主要有2项内容，需要人工合并的和需要AI合并的，让用户确认。确认OK后执行下一步。
3. 合并needmerge.diff，写入目标项目文件（先备份）。
4. **生成报告**：
   - 输出合并统计：成功数、跳过数、冲突数、需要人工合并的文件数。

## ⚠️ 注意事项
- 如果补丁格式无法解析，请立即停止并询问用户。


## 使用方式
用户输入：请合并 patch.diff 到当前项目